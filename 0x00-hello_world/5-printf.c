#include <stdio.h>

/**
 * this is a script
 * it prints a string
 * using the printf function
 */

int main(void)
{
	printf("%s", "with proper grammar, but the outcome is a piece of art,\n");
	return(0);
}
